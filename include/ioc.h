#ifndef IOC_H
#define IOC_H
#include <iostream>
#include <string.h>
#include <cctype>
#include <algorithm>
#include <vector>

bool isValid(std::string text);
float ComputeIndexOfCoincidence(std::string text);

#endif